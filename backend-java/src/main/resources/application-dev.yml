# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/electronic_wardrobe?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: root
    password: root
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2

  data:
    redis:
      host: localhost
      port: 6379
      password:

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  flyway:
    enabled: false

wechat:
  miniapp:
    app-id: wx13f5887a942ac750
    app-secret: dev-secret

logging:
  level:
    com.wardrobe: DEBUG
    org.springframework.web: DEBUG
